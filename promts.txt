1)
смотри вот структура датасета
[2 JSON файла отвечающие за полноту и метаданные датасетов]

наша задача сейчас написать препроцессинг.

1. считываем данные из разных таблиц
2. соединияем все в одно полотно
3. делаем feature engenireeng
4. храним все данные в общем feature store

оформляем все эти вещи как самостоятельные классы пишем краткий понятный и поддерживаемый код и начинаем вести makefile для автоматизации
не перегружай его излишним ф0оналом добавь туда просто этап препроцессинга
и пайтон я использую глобальный из user/ivan/myvenv

2) давай реализуем модель popularity-based с мониторингом ml-flow, которая будет читать лишь часть данных вместо всего хранилища для более простого тестирования и работы модели

3) давай начнем с малого, давай реализуем базовую страницу главную где будут наши самые популярные фильмы

пиши код как и ранее в виде классов по всем принципам, самое главное не усложняй код, там где это не нужно

4) напиши несколько документаций к каждому слою программы, должна быть общая директория docs/ там архитектура, веб с ручками, бекенд, документ по API и тд

5) давай поработаем с форматом страницы в вебе, хочу получить результат схожий с netflix

я хочу сделать страницу в несколько слоев

1. слой примерно центр экрана в котором будет самые популярные
2. персональные рекомендации, пока оставим его пустым или давай случайные туда подсуним
3. так же сделаем заготовку под каталог фильмов

пока не реализуй глубокий функционал, поработай над страницами и их стилями

6) давай реализуем теперь карточки для каждого фильма
что в них должно быть и реализуем загрузку постеров по внешнему API, вроде такой ф-ионал позволяет реализовать links.csv:

1. постер
2. описание
3. рейтинг
4. поле для отзыва
5. поле для оценки

7) проблемы с форматом картинки (поле для картинки явно меньше самой картинки, соотношение сторон не согласовано) идея с тем фильм открывается не в отдельной странице а поверзх хорошая, но добавь возможность перехода на страницу фильма как отдельная вкладка например по кнопке с превью, еще на главной странице хотел бы видеть сами постеры

хотел бы чтобы границы экрана были отдельны, то есть явные отступ, чтобы фильмы занимали не всю ширину экрана

сделай каждый фильм чуть более явным с точки зрения геометрии, чтобы все не сливалось в единое полотно,

8) отлчично, теперь move to page кнопку на уровень с save только правее сделай.

мне не очень нравится что картинка общего бекграунда не прогружается и давай добавим поиск по фильмам и жанрам, добавим фильтрацию

9) сделай более сложную системуфильтрации, вложенную и год и жанр и поля остальные поля доступные нам

10) отлично, с вебом пока будто все. давай начнем построение рекомендательной системы на основе колобаративной фильрации.

разработай рабочий пайплайн, используй уже готовые компоненты ETL процесса. Эндпоинты создадим позже, просто организуй процесс обучения модели

11) как обучать модели напиши доку

12) давай теперь уберем наивное разложение и заменим его  на ALS + CatBoost Ranker и так же сделаем модель на iALS + Catboost

13) окей персональные рекомендации и популярное работает, давай сделаем полный каталог фильмов, не будем ограничавать 76

14) поправь надпись Trending Now так немного отсуп слетел, он должен быть немного ниже и давай сделаем так:
у нас есть рекомендации пользовательские оставь там три юзера и четвертое поле в котором можно указать юзера.
