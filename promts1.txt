16) давай исправим docker-compose файл, чтобы можно было в контейнере запускать и backend и frontend,если что надо будет и внутри frontend поменять пути к backend которые потом настроим, если нужно добавь nginx и еще не забудь поменять docker файл у frontend и не забудь смотреть в #file:docs
17) давай реализуем frontend для логина и регистрации юзера придерживаясь нашего дизайна который уже есть(netflix), вход и регистрацию, потом уже сделаем backend я скажу когда; так давай уберем все что связанно с авторизацией просто оставим страницы регистрации и логина
18) давай сделаем страницу с пользователем 1) хотел просмотренные фильмы явно видеть на странице 2) вотчлист (фильмы которые хочу посмотреть) 3) оцененные фильмы, оставленные рецензии (отзывы) 4) если хочеш можем реализовать какой то график для статистики, соблюдай стиль, который уже имеется в вебе, должен быть полноформатным, закругленным и тд
22) давай поднимем postgres в docker-compose
24) давай поменяем docker файл и будем прокидывать по другому порты просто вот так; ports: - "5432:5432"
27) давай сделаем миграцию и создадим таблицу users, примерно там должны быть такие атрибуты, id, login, email, password_hash(чтобы проверять когда будет делать авторизацию через JWT токены), role(user, admin), created_at, давай посмотрим что получилось
29) давай уберем все что написали и сделаем миграции через flask и давай писать миграцию в файле .sql чтобы было легче и также запускать миграцию нужно в main.py в backend
31)давай создадим миграцию для создания таблицы watchlist таблица фильмов которые хочет посмотреть человек хотелось бы атрибуты id, movie_id, user_id и дополнительные поля которые описывают фильм
    title: str
    genres: Optional[str]
    year: Optional[int]
    avg_rating: Optional[float]
    num_ratings: Optional[int]
    popularity_score: Optional[float]
    tmdb_id: Optional[int] = None
    imdb_id: Optional[str] = None удачи
32) давай создадим миграцию для создания таблицы reviews, таблица для оценок фильмов примерно такие атрибуты id, movie_id, user_id, оценка фильма, отзыв, можешь накидать еще атрибутов
33) давай сделаем регистрацию и авторизацию пользователей будем использовать JWT для авторизации если что сохраняй пользователей в таблицу users, нужно реализовать роли user и admin, но регистрация только юзеров потому что админов я буду добавлять сам в бд чтобы потом пользоваться admin панелью, давай попробуем посмотреть что ты сделаешь
34) давай изменим frontend прикрутим ручки авторизации и чтобы подгружался профиль из бд потом сделаем апишку для отзывов и watchlist
35) давай теперь сделаем апишку для reviews и watchlist
36) так давай что то придумаем с watched (с просмотренными фильмами сейчас это все хранится локально) но хотелось бы реализовать отдельную таблицу под просмотренные фильмы потому что потом другой разработчик будет на основе просмотра фильма + отзыва обучать модель и также сделай для этого апишку