FROM node:20-alpine
WORKDIR /app
COPY package.json .
RUN npm install
COPY . .
EXPOSE 3000
# --hostname 0.0.0.0 обязателен, иначе Next.js слушает только 127.0.0.1 внутри контейнера
CMD ["npx", "next", "dev", "--hostname", "0.0.0.0", "--port", "3000"]
